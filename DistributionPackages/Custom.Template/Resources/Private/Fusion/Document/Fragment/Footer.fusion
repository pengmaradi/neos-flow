prototype(Custom.Template:Document.Fragment.Footer) < prototype(Neos.Fusion:Component) {
    renderer = Neos.Neos:ContentCollection {
        @context.node = ${q(site).children('footer').get(0)}
        attributes.class = 'footer-content'
    }
}