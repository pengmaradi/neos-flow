prototype(Custom.Template:Document.Fragment.Content.Footer) < prototype(Neos.Fusion:Component) {
    renderer = Neos.Neos:ContentCollection {
        nodePath = ${q(site).children('footer').property('_path')}
        collection = ${q(side).children('footer').children()}
    }
}