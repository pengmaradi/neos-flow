// TODO Neos.Neos-20251005080230 Line 8: You may need to rewrite "VARIABLE.path" to Neos.Node.path(VARIABLE). We did not auto-apply this migration because we cannot be sure whether the variable is a Node.
// TODO Neos.Neos-20251005080230 Line 14: You may need to rewrite "VARIABLE.path" to Neos.Node.path(VARIABLE). We did not auto-apply this migration because we cannot be sure whether the variable is a Node.
// TODO Neos.Neos-20251005080230 Line 19: You may need to rewrite "VARIABLE.path" to Neos.Node.path(VARIABLE). We did not auto-apply this migration because we cannot be sure whether the variable is a Node.
prototype(Custom.Template:Helper.StyleTag) < prototype(Neos.Fusion:Component) {
    path = null
    defer = true

    @if.hasPath = ${this.path}

    renderer = Neos.Fusion:Tag {
        tagName = 'link'

        @context.srcInternal = Neos.Fusion:ResourceUri {
            path = ${props.path}
        }

        attributes {
            rel = 'stylesheet'
            href = ${!String.startsWith(props.path, 'resource:') ? props.path : srcInternal}
            media = ${props.media ? props.media : 'all'}
        }
    }
}
